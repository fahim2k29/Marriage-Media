! function(c) {
    var h = c.event.dispatch || c.event.handle,
        s = c.event.special,
        n = "D" + +new Date,
        o = "D" + (+new Date + 1),
        a = "D" + (+new Date + 2);
    s.scrollstart = {
        setup: function(e) {
            var o, a = c.extend({
                    latency: s.scrollstop.latency
                }, e),
                t = function(e) {
                    var t = arguments;
                    o ? clearTimeout(o) : (e.type = "scrollstart", h.apply(this, t)), o = setTimeout(function() {
                        o = null
                    }, a.latency)
                };
            c(this).bind("scroll", t).data(n, t)
        },
        teardown: function() {
            c(this).unbind("scroll", c(this).data(n))
        }
    }, s.scrollstop = {
        latency: 250,
        setup: function(e) {
            var a, n = c.extend({
                    latency: s.scrollstop.latency
                }, e),
                t = function(e) {
                    var t = this,
                        o = arguments;
                    a && clearTimeout(a), a = setTimeout(function() {
                        a = null, e.type = "scrollstop", h.apply(t, o)
                    }, n.latency)
                };
            c(this).bind("scroll", t).data(o, t)
        },
        teardown: function() {
            c(this).unbind("scroll", c(this).data(o))
        }
    }, s.scrollslow = {
        threshold: 1e3,
        setup: function(e) {
            var l, i = c.extend({
                    threshold: s.scrollslow.threshold
                }, e),
                d = c(window).scrollTop(),
                r = (new Date).getTime(),
                t = function(a) {
                    var n = this,
                        s = arguments;
                    l || (l = setTimeout(function() {
                        l = null, a.type = "scrollslow";
                        var e = (new Date).getTime() - r,
                            t = c(window).scrollTop() - d,
                            o = Math.abs(t / e);
                        r = (new Date).getTime(), d = c(window).scrollTop(), 1e3 * o < i.threshold && h.apply(n, s)
                    }, 100))
                };
            c(this).bind("scroll", t).data(a, t)
        },
        teardown: function() {
            c(this).unbind("scroll", c(this).data(a))
        }
    }
}(jQuery), $(document).ready(function() {
    $(".sectionHomeSlider > ul").cycle({
        fx: "fade",
        speed: 2500,
        slideResize: !1,
        containerResize: !1,
        fit: 0,
        timeout: 5e3,
        before: function(e, t, o) {
            2 == o.nextSlide ? $(".rightSection").addClass("changeColor") : 3 == o.nextSlide ? $(".rightSection").addClass("changeColor") : $(".rightSection").removeClass("changeColor")
        }
    }), $("[id^=detail-]").hide(), $(".toggle").click(function() {
        $input = $(this), $target = $("#" + $input.attr("data-toggle")), $target.slideToggle(), 1 == $input.find(".glyphicon-chevron-down").length ? $input.find(".glyphicon-chevron-down").removeClass("glyphicon-chevron-down").addClass("glyphicon-chevron-up") : 1 == $input.find(".glyphicon-chevron-up").length && $input.find(".glyphicon-chevron-up").removeClass("glyphicon-chevron-up").addClass("glyphicon-chevron-down")
    }), $.expr[":"].onscreen_search = function(e) {
        var t = $(window),
            o = t.scrollTop(),
            a = t.height(),
            n = o + a,
            s = $(e),
            l = s.offset().top,
            i = s.height(),
            d = l + i;
        return o <= l && l < n || o < d && d <= n || a < i && l <= o && n <= d
    };
    var t = $(".results-page");
    $("#output");
    var i = function(e) {
            var n = 0,
                s = 0;
            null == e && (e = !1), t.removeClass("onscreen_search preload").addClass("offscreen_search"), t.filter(":onscreen_search").each(function(e, t) {
                var o = $(this),
                    a = parseInt(o.data("page"));
                0 == e && 0 < a - 1 && (n = a - 1), s = a + 1, o.removeClass("offscreen_search").addClass("onscreen_search")
            }), $("#results-page-visitors" + n + ",#results-page-visitors" + s).addClass("preload").removeClass("offscreen_search"), o(e)
        },
        o = function(e) {
            1 == e ? ($(".onscreen_search").trigger("contentLoadFromData"), $(".preload").trigger("contentLoadFromData")) : ($(".onscreen_search").trigger("contentLoad"), $(".preload").trigger("contentLoad")), $(".offscreen_search").trigger("contentUnload")
        };
    ! function() {
        var a = $("#photoBoxHolder_searchvisitor"),
            o = parseInt(a.data("startpage")),
            n = "500px",
            s = "500px";
        t.each(function() {
            $(this).addClass("unloaded"), $(this).on("contentLoad", function() {
                var t = $(this),
                    e = parseInt(t.data("page")),
                    o = a.data("searchurl") + "&page=" + e;
                t.hasClass("isLoaded") || t.hasClass("loading") || (t.addClass("loading"), null == t.data("content") ? $.get(o).done(function(e) {
                    t.data("content", e), t.html(e).addClass("isLoaded").removeClass("unloaded").css({
                        height: "auto",
                        width: "auto"
                    }), n = t.height(), s = t.width(), $('[data-toggle="tooltip"]').tooltip()
                }).always(function() {
                    t.removeClass("loading")
                }) : (t.html(t.data("content")).addClass("isLoaded").removeClass("loading").removeClass("unloaded").css({
                    height: "auto",
                    width: "auto"
                }), n = t.height(), s = t.width()))
            }), $(this).on("contentLoadFromData", function() {
                var e = $(this);
                e.hasClass("isLoaded") || e.hasClass("loading") || null != e.data("content") && e.html(e.data("content")).addClass("isLoaded").removeClass("loading").removeClass("unloaded").css({
                    height: "auto",
                    width: "auto"
                })
            }), $(this).on("contentUnload", function() {
                $(this).empty().removeClass("isLoaded").removeClass("loading").addClass("unloaded").css({
                    height: n,
                    width: s
                })
            })
        });
        var l = $("#results-page-visitors" + o);
        l.length && (l.addClass("loading"), $.get(a.data("searchurl") + "&page=" + l.data("page")).done(function(e) {
            var t;
            l.data("content", e), l.html(e).addClass("isLoaded").removeClass("unloaded").css({
                height: "auto",
                width: "auto"
            }), n = l.height(), s = l.width(), $(".unloaded").css({
                height: n,
                width: s
            }), 1 < o && $(window).scrollTop(l.offset().top - 55), $(".scrollable").add(window).on("scrollstop", i), $(".scrollable").add(window).on("scrollslow", i), $(".scrollable").add(window).on("scroll", function() {
                t || (t = setTimeout(function() {
                    i(!0), t = null
                }, 100))
            }), i()
        }).always(function() {
            l.removeClass("loading")
        }))
    }()
}), $(document).ready(function() {
    var o = !0;
    $(".cmsContent").bind("scroll", function() {
        var e = $(".load-more").attr("lastID"),
            t = $(".load-more").attr("typeid");
        e <= $(".load-more").attr("totalnumberofpages") && $(this).scrollTop() + $(this).innerHeight() >= $(this)[0].scrollHeight && 0 != e && o && (o = !1, $.ajax({
            type: "GET",
            url: "/app/webview/successstoriesajax/" + t + "/" + e,
            beforeSend: function(e) {
                $(".load-more").show()
            },
            success: function(e) {
                $(".load-more").remove(), $(".cmsContent").append(e), o = !0
            }
        }))
    })
});
//# sourceMappingURL=CmsController.min.js.map